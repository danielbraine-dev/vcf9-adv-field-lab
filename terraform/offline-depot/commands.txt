# base tooling
sudo apt update -y
sudo apt install -y jq unzip bash-completion gnupg software-properties-common
sudo snap install curl
curl -L "https://github.com/vmware/govmomi/releases/latest/download/govc_$(uname -s)_$(uname -m).tar.gz" -o "govc.tar.gz"
sudo tar -xvzf govc.tar.gz -C /usr/local/bin
sudo chmod +X /usr/local/bin/govc

# vim tidy-up (optional)
sudo apt -y remove vim-common || true
sudo apt clean
sudo apt purge -y || true
sudo apt -y update
sudo apt -y install vim
